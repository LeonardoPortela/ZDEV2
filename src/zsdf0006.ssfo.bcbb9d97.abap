SELECT SINGLE BEZEI
  FROM T005U
  INTO ESTADO
  WHERE SPRAS EQ SY-LANGU
    AND LAND1 EQ 'BR'
    AND BLAND EQ WA_IMPORT-UF.

DESCRICAO = |{ DESCRICAO CASE = LOWER }|.
ML_SJ  = |{ ML_SJ CASE = LOWER }|.

SELECT SINGLE LTX
  FROM T247
  INTO MES
  WHERE SPRAS EQ SY-LANGU
      AND MNR EQ WA_IMPORT-MES.

SELECT SINGLE LTX
  FROM T247
  INTO VENC_MES
  WHERE SPRAS EQ SY-LANGU
      AND MNR EQ WA_IMPORT-VENC_MES.


  if WA_IMPORT-situacao eq 'união estável'.
   EC_CONJUGE = 'convivente'.
   else.
    EC_CONJUGE = WA_IMPORT-situacao.
    ENDIF.

