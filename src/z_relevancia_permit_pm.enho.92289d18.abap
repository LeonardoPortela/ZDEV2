"Name: \PR:SAPLIMSP\FO:LIMSPF00_PERMIT_DELETE\SE:BEGIN\EI
ENHANCEMENT 0 Z_RELEVANCIA_PERMIT_PM.
* Impedir que permits possam ser removidas, não conseguiram faze-lo por perfil.

  DATA: LV_AUART    TYPE AUFK-AUART,
          WL_SCRGNTAB TYPE IMPRM_SCRGNWA_TYPE.

  CLEAR: WL_SCRGNTAB.
  READ TABLE SCRGNTAB INTO WL_SCRGNTAB INDEX 1.
  IF SY-SUBRC IS INITIAL.
    SELECT SINGLE AUART
      FROM AUFK
      INTO LV_AUART
      WHERE AUFNR EQ WL_SCRGNTAB-AUFNR.

    IF LV_AUART EQ 'ZPM7'.
      MESSAGE 'Permits não pode ser removidas.' TYPE 'S'.
      CHECK 1 = 0.
    ENDIF.
  ENDIF.
ENDENHANCEMENT.
